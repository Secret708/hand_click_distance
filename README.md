# Программа для отлеживания руки и расчёта дистанции между указательным и большим пальцем

## Кратко, что делает программа:

- Программа отслеживает руку с помощью [MediaPipe](https://github.com/google/mediapipe) и рассчитывает дистанцию между большим и указательным пальцем. Если эта дистанция больше определённого числа (оно записывается в константе DISTANCE_FOR_CLICK), то программа делает клик с помощью библиотеки [Pynput](https://github.com/moses-palmer/pynput)

## История создания программы:

- Идея для этой программы мне пришла, когда я увидел эдит в тик токе. На эту программу я потратил 3 часа (учитывая последующие улучшения). Предназначена она для игры Google динозаврик (именно эту игру показывали в эдите), но я считаю, что её можно использовать не только для этого. Воспользовался всеми своими силами и знаниями, чтобы её сделать понятной и хорошей (надеюсь получилось). Также поставил некоторые ограничения в программе (но их можно убрать)

## Технологии:

- [MediaPipe](https://github.com/google/mediapipe) — для отслеживания ключевых точек руки
- [OpenCV](https://github.com/opencv/opencv) — для захвата и обработки видео
- [NumPy](https://github.com/numpy/numpy) — для работы с массивами
- [screeninfo](https://github.com/rr-/screeninfo) — для получения размеров мониторов
- [pynput](https://github.com/moses-palmer/pynput) — для эмуляции кликов мыши
- [time](https://docs.python.org/3/library/time.html) — для работы с таймерами (задержка между кликами)
- [math](https://docs.python.org/3/library/math.html) — для математических операций (вычисление расстояния)

- Python 3.10

## За что отвечают параметры, получаемые классом:

- PATH_TO_OUTPUT_VIDEO - путь для сохранения видео. По умолчанию: output_video_hand_click.mp4

- MIN_DETECTION_CONFIDENCE - минимальный порог уверенности для нейросети (от 0.0 до 1.0). По умолчанию 0.7

- MIN_TRACKING_CONFIDENCE - минимальный порог уверенности для отслеживания руки (от 0.0 до 1.0). По умолчанию 0.6

- MAX_NUM_HANDS - максимальное кол-вл рук, которое может отследить mediapipe (до 20 включительно). По умолчанию 1

- FRAME_WIDTH и FRAME_HEIGHT - размер монитора. Проверяется с помощью screeninfo. По умолчанию FRAME_WIDTH = 1200, а FRAME_HEIGHT = 700

- CIRCLE_RADIUS - радиус кругов (до 15). По умолчанию 4

- MONITOR - монитор, который будем использовать для записи видео. Проверяется с помощью screeninfo. По умолчанию 0 

- THICKNESS и THICKNESS_TEXT - толщина линий для соединения пальцев и толщина для текста (до 10). По умолчанию 2 и 3

- DISTANCE_FOR_CLICK - дистанция, которая требуется для произведения клика (до 130, но рекомендую менять в зависимости от размеров экрана). По умолчанию 80

- CLICK_COUNT - сколько кликов делать, если дистанция будет больше DISTANCE_FOR_CLICK (до 3 кликов). По умолчанию 1

- CLICK_COOLDOWN - перезарядка клика (до 1 секунды). По умолчанию 1 секунда

- MOUSE_BUTTON - кнопка на мышке, которая будет осуществлять клик (могут быть только left, right, middle, x1, x2). По умолчанию left

## Установка
1. Клонируй репозиторий:
```bash
git clone https://github.com/Secret708/hand_click_distance.git
```
2. Настройка виртуального окружения:
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate
```
4. Установите зависимости:
```bash
pip install -r requirements.txt
```
5. Запусти
```bash
python hand_click_distance.py
```
## Поддержи автора 

- 💬 [Telegram](https://t.me/programmingbasicschan)
- 📺 [TikTok](https://tiktok.com/@code_basics)


## Буду рад вашей подписке!
